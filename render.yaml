services:
  - type: web
    name: codeiq-backend
    env: docker
    dockerfilePath: ./web/backend/Dockerfile
    healthCheckPath: /health
    envVars:
      - key: PYTHONPATH   # This non-secret variable is fine here
        value: /app